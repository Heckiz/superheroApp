import React, {FC} from 'react';
import {Text} from 'react-native';
import styles from '../styles';

const Register: FC = () => {
  return <Text style={styles.title}>Register</Text>;
};

export default Register;
