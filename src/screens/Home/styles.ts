import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 25,
  },
  charactersList: {
    display: 'flex',
    flexDirection: 'row',
  },
});

export default styles;
